location /cyberchef/ {
  alias /app/tools/cyberchef/dist/;
  index index.html;
  try_files $uri $uri/ =404;
  location ~* \.(js|css|woff2?|ttf|png|svg|wasm|json)$ { expires 30d; add_header Cache-Control public; }
}
